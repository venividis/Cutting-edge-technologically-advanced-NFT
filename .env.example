# RPC endpoints
RPC_ETHEREUM=
RPC_BASE=
RPC_ARBITRUM=
RPC_OPTIMISM=

# Deployer (use a hardware wallet or KMS in production; never commit a key)
DEPLOYER_PRIVATE_KEY=

# LayerZero V2 endpoints (per-chain) — see https://docs.layerzero.network/v2/deployments/deployed-contracts
LZ_ENDPOINT=

# Verification
ETHERSCAN_API_KEY=
